.nav {
    background-color: #008;
    height: 3em;
}

img {
    height: 10em;
}

h1 {
    margin: 0;
}

button {
    background-color: #d24c5b;
    color: #fff;
    border: none;
    padding: 0.75em;
    border-radius: 5px;
    cursor: pointer;
}

pre {
    color: #fff;
    background-color: #333;
    padding: 1em;
    border-radius: 5px;
    white-space: normal;
    word-wrap: break-word;
    word-break: break-all;
}

button:hover {
    background-color: #df5867;
    box-shadow: 2.5px 2.5px 5px #e2e2e2;
}

.execs {
    display: flex;
    justify-content: space-around;
    align-items: center;
}

.exec {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    gap: 1em;
    background-color: #e2e2e2;
    border-radius: 10px;
    width: min-content;
    padding: 1em;
    height: min-content;
}

.exec > p {
    margin: 0;
}

:not(.div-collapsed) > .hidable-img {
    display: none;
}

.os-div {
    display: flexbox;
    background-color: #fff;
    padding: 3%;
    flex-grow: 0;
    flex-shrink: 1;
    flex-basis: 1;
    transition: all 0.5s;
}

.div-collapsed {
    width: 15% !important;
    min-width: 3em;
    display: flex;
    flex-direction: column;
    justify-content: space-around;
    align-items: center;
    cursor: pointer;
    transition: all 0.5s;
}

.div-collapsed > .hidable-img {
    display: flexbox !important;
    width: 80% !important;
    height: auto !important;
}

.div-collapsed > :not(.hidable-img) {
    display: none;
}

.content {
    padding: 5%;
    display: flex;
    justify-content: space-between;
    align-content: space-between;
    flex-wrap: nowrap;
    gap: 5%;
    width: 90%;
    height: max-content;
}

body {
    background-color: #333;
    padding: 0;
    margin: 0;
    font-family: sans-serif;
}
