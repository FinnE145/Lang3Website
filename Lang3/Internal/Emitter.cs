namespace Lang3;

class Emitter(Parser.Node root, string code, string fp) {
    private readonly Parser.Node root = root;
    private readonly string code = code;
    private readonly string fp = fp;

    public void Emit() {
        
    }
}